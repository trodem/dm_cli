# Workflow

1. Pull latest: `git pull`
2. Create branch: `git checkout -b feature/xyz`
3. Work and commit: `git add . && git commit -m "feat: xyz"`
4. Push: `git push -u origin feature/xyz`
5. Open PR
