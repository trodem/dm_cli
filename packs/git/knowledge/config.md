# Config

- List: `git config --list`
- Set user: `git config --global user.name "Name"`
- Set email: `git config --global user.email "me@example.com"`
- Set editor: `git config --global core.editor "code --wait"`
